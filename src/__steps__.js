/**
 *         CONTEXT API(context api er step 3 ti)
 * 1.create context (export)
 * 2.create provider
 * 3.set context value
 * 4.to access the value: useContext hook
 * */

/**
 *          SHARED CONTEXT API
 * 1.main.jsx set AuthProvider
 * 2.inside providers access children props and then use
 * */ 

/**
 * 1.break down the create user function
 * 2.auth provider will have auth and email and password from register
 * */ 